{
  "name": "unaadeb-monorepo",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "type-gen": "turbo run type-gen",
    "mobile": "pnpm --filter mobile start",
    "api": "pnpm --filter api dev",
    "admin": "pnpm --filter admin dev"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "~5.9.2"
  }
}
